[MASTER]
init-hook="from pylint.config import find_default_config_files; import sys; sys.path.append(next(find_default_config_files()).parent.as_posix()); sys.path.append(next(find_default_config_files()).parent.as_posix() + '/chatbot/bot/plugins'); "
jobs=0

[MESSAGES CONTROL]
disable=C,
      syntax-error,
      too-few-public-methods,
      global-statement,
      useless-object-inheritance,
      try-except-raise,
      broad-except,
      too-many-branches,
      too-many-arguments,
      protected-access,
      chained-comparison,
      duplicate-code,
      cyclic-import,
      unspecified-encoding

[REPORTS]
output-format=colorized

# vim: ft=conf
